node_modules/
dist/
.DS_Store
*.local
.screenshots/

# Feed artifacts generated by the Actions snapshot scripts (published to
# their data branches, never committed to main)
news.json
news-prev.json
values-history.json
values-archive.json
trade-values.json
values-consensus.json
rookie-intel.json
.cache/

# NOTE: api/ is deliberately NOT ignored. Vercel detects functions from the
# source tree and traces rather than bundles, so the esbuild output is
# committed; ci.yml rebuilds and diffs it to prevent drift.
public-mcp/
