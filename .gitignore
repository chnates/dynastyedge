node_modules/
dist/
.DS_Store
*.local
.screenshots/

# Feed artifacts generated by the Actions snapshot scripts (published to
# their data branches, never committed to main)
news.json
news-prev.json
values-history.json
values-archive.json
trade-values.json
rookie-intel.json
.cache/

# scripts/build-mcp.mjs's verification bundle (api/mcp.js is a COMMITTED shim —
# Vercel detects functions from the source tree, not from build output)
.mcp-build/
public-mcp/
